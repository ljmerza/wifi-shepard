# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/

# Tooling caches
.ruff_cache/
.pytest_cache/
.mypy_cache/
.pyright/
.coverage
coverage.xml
htmlcov/

# uv
uv.lock.bak

# Local state (runtime artifacts the daemon writes)
*.db
*.db-journal
*.db-shm
*.db-wal

# Secrets & local env
# Real env files used by docker-compose.yml (never commit secrets).
# *.env.example is unaffected -- its suffix is .example, not .env.
*.env
*.local.env

# Runtime state written by the daemon (SQLite WAL, etc.).
volumes/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Claude Code per-developer settings
.claude/settings.local.json
